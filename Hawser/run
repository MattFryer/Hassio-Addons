#!/usr/bin/with-contenv bashio

TOKEN=$(bashio::config 'token')

exec docker run \
  --name hawser \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v /share/hawser/stacks:/data/stacks \
  -p 2376:2376 \
  -e TOKEN="${TOKEN}" \
  ghcr.io/finsys/hawser:latest
